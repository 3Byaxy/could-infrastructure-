# docker-compose environment variables
# Copy this file to .env and set POSTGRES_PASSWORD before running docker compose up.

POSTGRES_DB=appdb
POSTGRES_USER=appuser
# REQUIRED: set a strong password (at least 16 characters)
POSTGRES_PASSWORD=
